import type { SiteContent } from "./types";
import data from "./en.json";

// Ported verbatim from prototype/site-content.js (en) — do not edit copy here.
const en = data as unknown as SiteContent;

export default en;
