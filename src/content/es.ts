import type { SiteContent } from "./types";
import data from "./es.json";

// Ported verbatim from prototype/site-content.js (es) — do not edit copy here.
const es = data as unknown as SiteContent;

export default es;
